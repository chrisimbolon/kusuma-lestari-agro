# ═══════════════════════════════════════════════════════
#  PT. KUSUMA LESTARI AGRO — Backend Requirements
# ═══════════════════════════════════════════════════════

# Core
Django==5.1.4
djangorestframework==3.15.2
django-filter==24.3
django-cors-headers==4.6.0

# Auth
djangorestframework-simplejwt==5.3.1

# Database — SQLite for dev (zero config), swap to psycopg2 for prod
# psycopg2-binary==2.9.10   ← uncomment when you have PostgreSQL

# Environment
python-decouple==3.8

# Dev tools
django-extensions==3.2.3
ipython==8.30.0
